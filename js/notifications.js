// js/notifications.js
// Not wired up yet. Real email notifications require a backend (Cloud
// Function or similar) since credentials can't live in client JS — see
// the note in the original architecture plan. Not required for the
// current single-device version.
